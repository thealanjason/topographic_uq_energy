name: env-model
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - pandas
  - pyyaml
  - setuptools<70
  # --- Alumet Build Dependencies ---
  - rust
  - pkg-config
  - openssl
  # ---------------------------------
  - pip
  - pip:
    - synxflow